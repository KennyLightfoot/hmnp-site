/** @type {import('next').NextConfig} */
const nextConfig = {
  // Absolute minimal configuration for build troubleshooting
  typescript: {
    ignoreBuildErrors: true,
  },
  reactStrictMode: true,
  poweredByHeader: false,
}

export default nextConfig